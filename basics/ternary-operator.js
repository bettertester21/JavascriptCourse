{
    let text = (age < 18) ? "Minor" : "Adult";
    console.log(text); // Output: Minor
}


{ 
    let isMember = true;
    let discount = isMember ? 0.2 : 0;
    console.log(discount); // Output: 0.2
}


{ 
    let isMember = false;
    let discount = isMember ? 0.2 : 0;
    console.log(discount); // Output: 0
}

